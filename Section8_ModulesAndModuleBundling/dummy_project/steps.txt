Steps:
- npm init -y
- tsc --init
- configure tsc
- npm i -D typescript // -D for development only dependencies
- npm i -D @types/node
- add files
- configure npm scripts
// Need to make sure that package.json, .tsconfig.json, and module bundler are configured in sync with each other